it('should open a book pdf')
// const openBook = (path);

it('should add a query panel')
// const _displayQueryPanel = (el, title, authors, path);

it('should arr a book query result')
// const _displayBookQueryResult = (el, json, path);

it('should add an empty query warning')
// const _displayEmptyQueryResult = (el);

it('should remove a book')
// const removeBook = async ({path});

it('should import a book')
// const importBook = ();

it('should update a book')
// const updateBook = async ({title, authors, path});

it('should add a button')
// const _addButton = (el, name, fn);

it('should display all books')
// const displayAllBooks = (bookcaseEl);

it('should query all books')
// const queryAllBooks = async ();

it('should make the toolbar')
// const makeToolbar = ();

it('should make the bookcase')
// const makeBookcase = ();

it('should init the client')
// const init = async ();
